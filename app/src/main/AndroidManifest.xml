<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ter_nak">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar.Ternak">
        <activity android:name=".description.DescriptionAyamkubActivity"></activity>
        <activity android:name=".description.DescriptionAyampetelurActivity" />
        <activity android:name=".description.DescriptionAyamkampungActivity" />
        <activity android:name=".description.DescriptionAyambroilerActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".product.ProdukAyamkubActivity" />
        <activity android:name=".product.ProdukAyampetelurActivity" />
        <activity android:name=".product.ProdukAyambroilerActivity" />
        <activity android:name=".product.ProdukAyamkampungActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>